# AI-Vaani — Python dependencies
# Install with: pip install -r requirements.txt

# Core framework
streamlit>=1.37.0

# Environment variable loading
python-dotenv>=1.0.0

# LLM inference (Groq — free tier, 500+ tok/sec)
groq>=0.9.0

# HTTP client (kept for potential future use)
requests>=2.31.0

# Audio resampling (compress to 16kHz mono before STT upload)
pydub>=0.25.1

# Numerical audio processing
numpy>=1.26.0

# Optional: Google Cloud STT alternative
# google-cloud-speech>=2.26.0

# Optional: local audio file support
# soundfile>=0.12.1
# librosa>=0.10.2
